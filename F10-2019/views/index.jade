extends layout

block content
  h1= title
  p Welcome to #{title}
  form(method='POST' action='/theme')
  div.input
    div.form-group
      label(for='Theme') Theme:
      input#favoriteBook.form-control(type='text', placeholder='Themes you beleive in' name='favoriteBook')
  input(type="submit", value="Submit")
